# Accessibility Foundation

Every new UI must meet WCAG 2.1 AA.

Required checks:

- Keyboard navigation for all interactive controls.
- Screen-reader labels for icon-only controls.
- Text alternatives for ML results and visual-only confidence indicators.
- Color must not be the only way to communicate status.
- New user-facing strings must be ready to move into translation catalogs.
